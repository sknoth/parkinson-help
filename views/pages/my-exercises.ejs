<!DOCTYPE html>
<html lang="en">
<% include ../partials/head %>
<body class="container">
  <div class="page-header text-center">
    <h1><span class="fa fa-medkit"></span> Patient Dashboard</h1>
  </div>
  <% include ../partials/header %>

  <main>
      <div class="alert alert-info" role="alert">
        On this page  a patient can get information on his or her treatments and tests.
      </div>
      <% if (typeof user === 'undefined') { %>
        <div class="alert alert-danger" role="alert">
          You need to be logged in to view this page.
        </div>
      <% } else {%>
<div class="row">
  <div class="col-md-6">
        <h3>My Test Sessions</h3>
        <div>
          <p>Completed Test Sessions</p>
          <ul class="list-inline">
            <li><b>per day:</b> xxx sessions</li>
            <li><b>per week:</b> xxx sessions</li>
            <li class="active"><b>per month:</b> xxx sessions</li>
            <div>TODO: INSERT BAR CHART HERE!!!</div>
          </ul>
        </div>

        <ul class="list-unstyled list-striped">
          <% for(var i in testSessions) { %>
            <li>
              <p><b>Name</b>: <%= testSessions[i].therapyName %></p>
              <p><b>Date</b>: <%= testSessions[i].test_datetime[0] %></p>
              <p><b>Medicine</b>: <%= testSessions[i].medicineName %></p>
              <p><b>Dosage</b>: <%= testSessions[i].dosage %></p>
              <p><a>show test results for <%= testSessions[i].dataURL %></a></p>
            </li>
          <% } %>
        </ul>
      <% } %>
</div>
  <div class="col-md-6">
        <h3>Recommended Videos</h3>
        <% videoIds.forEach(function(videoId) { %>
          <div class="ph-video">
            <div class="embed-responsive embed-responsive-16by9">
              <iframe width="420" height="315"
                  src="https://www.youtube.com/embed/<%= videoId %>"
                  frameborder="0" allowfullscreen></iframe>
            </div>
          </div>
        <% }); %>
</div>
</div>
      <% include ../partials/footer %>

      <script>
      console.log(<%- JSON.stringify(testSessions) %>);
      </script>
    </main>
  </body>
</html>
